/**
 * XXX
 * @param {Client} client
 * @param {Interaction} interaction
 * @param {string} id
 */
module.exports = (client, message, id) => {
  // TODO - check that something is playing
  // yes - stop
  // no - message out no sound is playing
}